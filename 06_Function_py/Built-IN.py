
def FullName():
    """this Function takes the input from the user | returns the Full Name """
    name = input("enter the Full Name!")
    return name

print(FullName.__doc__)
print(FullName.__name__)
